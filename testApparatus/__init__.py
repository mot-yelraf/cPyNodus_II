"""Group manual apparatus modules for on-device diagnostic runs.

These modules are operator-invoked probes rather than host pytest tests. Most
require CircuitPython hardware APIs and should run only while the normal
firmware application is stopped.
"""
